# CI/CD optimized configuration - remove problematic store-dir
# Let pnpm use default store location

# Enable strict peer dependencies checking
strict-peer-dependencies=false

# Auto-install peer dependencies
auto-install-peers=true

# Use faster node-modules linker for development
node-linker=hoisted
