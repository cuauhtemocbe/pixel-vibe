# Store packages in a writable directory for CI/CD
# Use /tmp in CI environments or ~/.pnpm-store locally
store-dir=~/.pnpm-store

# Enable strict peer dependencies checking
strict-peer-dependencies=false

# Auto-install peer dependencies
auto-install-peers=true

# Use faster node-modules linker for development
node-linker=hoisted
